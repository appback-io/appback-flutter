name: example
description: A new Flutter project.
version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  app_back:
    path: ../../app_back

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true